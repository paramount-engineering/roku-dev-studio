/**
 * Vendored ESM copied from `modern-screenshot` at build time (see transpile-renderer.ts).
 * Matched by `../../vendor/modern-screenshot.mjs` from `components/dev-app/*` (same relative path under `dist/`).
 */
declare module '../../vendor/modern-screenshot.mjs' {
  export function domToPng(node: Node, options?: Record<string, unknown>): Promise<string>;
}
