/**
 * Declaration-side companion for the vendored ESM file `tanstack-virtual-core.mjs`.
 * Re-export the upstream package types so TypeScript can type relative `.mjs`
 * imports that point at the generated vendor file.
 */
export * from '@tanstack/virtual-core';
